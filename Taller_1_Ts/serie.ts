export class Serie {

    constructor(public idSerie: number, public name: string, public channel: string, public seasons: number, public description: string, public webSite: string, public img: string) {
    }
    
}